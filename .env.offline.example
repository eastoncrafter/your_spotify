# Example Environment Configuration for Offline Mode
# Copy this to .env and adjust as needed

# ===========================================
# OFFLINE MODE CONFIGURATION
# ===========================================

# Enable offline/read-only mode (no Spotify API calls, no imports, no write operations)
OFFLINE_MODE=true

# Optional: Specify a user ID to use (otherwise uses first user in database)
# OFFLINE_DEV_ID=507f1f77bcf86cd799439011

# ===========================================
# DATABASE CONFIGURATION
# ===========================================

# MongoDB connection string (must contain existing data)
MONGO_ENDPOINT=mongodb://localhost:27017/your_spotify

# Set to true if MongoDB user doesn't have admin rights
# MONGO_NO_ADMIN_RIGHTS=false

# ===========================================
# SERVER CONFIGURATION
# ===========================================

# API server port
PORT=8080

# Client/frontend URL
CLIENT_ENDPOINT=http://localhost:3000

# API endpoint (backend URL)
API_ENDPOINT=http://localhost:8080

# ===========================================
# SPOTIFY CONFIGURATION
# (Still required for schema validation, but not used in offline mode)
# ===========================================

# Spotify OAuth credentials (can be dummy values in offline mode)
SPOTIFY_PUBLIC=dummy_client_id
SPOTIFY_SECRET=dummy_client_secret

# ===========================================
# OPTIONAL CONFIGURATION
# ===========================================

# JWT cookie validity (default: 1h)
# COOKIE_VALIDITY_MS=1h

# Log level: debug, info, warn, error (default: info)
# LOG_LEVEL=info

# Node environment: development or production
# NODE_ENV=production

# Timezone for server logs (optional)
# TIMEZONE=America/New_York

# CORS origins (comma-separated, or leave default)
# CORS=http://localhost:3000,http://localhost:8080

# Maximum import cache size (not used in offline mode)
# MAX_IMPORT_CACHE_SIZE=50000

# Prometheus metrics authentication (optional)
# PROMETHEUS_USERNAME=admin
# PROMETHEUS_PASSWORD=secret

# ===========================================
# NOTES FOR OFFLINE MODE
# ===========================================

# 1. You MUST have existing data in MongoDB before enabling offline mode
# 2. No new data will be imported or synced while in offline mode
# 3. All write operations (settings changes, imports, etc.) will be blocked
# 4. The app will automatically log in as the first user in the database
# 5. Spotify OAuth credentials are still required for validation but won't be used
# 6. Background tasks (music syncing, import cleanup) will not run
# 7. This mode is for viewing historical statistics only

# To populate data before enabling offline mode:
# 1. Set OFFLINE_MODE=false
# 2. Run the app normally and import your data
# 3. Wait for data to sync from Spotify
# 4. Once you have sufficient data, set OFFLINE_MODE=true
# 5. Restart the server
